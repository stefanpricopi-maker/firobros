# Folosit la `npm run build` local pentru canonical, sitemap, robots.txt și JSON-LD (pe Netlify: și `URL`).
PUBLIC_SITE_URL=https://numele-tau.netlify.app

# Titluri, descrieri, adresă, telefon: editează `src/data/seo-config.ts`.
# După deploy: actualizează și linia Sitemap din `public/robots.txt` cu același domeniu.
